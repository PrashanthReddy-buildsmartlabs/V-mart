rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      // Anyone can view product images. Only authenticated users can upload.
      allow read: if true;
      allow write: if request.auth != null; 
    }
  }
}
